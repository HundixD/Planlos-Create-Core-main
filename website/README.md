# Website

Dieser Ordner wird im nächsten Entwicklungsschritt mit der Planlos-Create-Website gefüllt.

Die Website wird als eigener Node.js-Dienst betrieben und ruft die geschützte API des Bots serverseitig auf. Der API-Schlüssel wird niemals an den Browser ausgeliefert.
